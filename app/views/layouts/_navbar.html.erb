<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="\">Internet Vortex [alpha]</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to "Archive", front_page_archive_path(DateTime.now.yesterday.strftime('%B-%d-%Y')) %></li>
        <li><a href="/about">About</a></li>
        <% if @archive %>
          <li>
            <%= render 'layouts/date_picker', { channel: @channel, css: "" } %>
          </li>
        <% end %>
        <li class="nav-share"><span class='st_sharethis_large' displayText='ShareThis'></span></li>
      </ul>
      <% if false %>
        <!-- no facebook until launch -->
        <div id='social-container'>
          <div class="fb-like" data-share="true" data-width="450" data-show-faces="true"></div>
        </div>
      <% end %>
    </div><!--/.nav-collapse -->
  </div>
</nav>

<% if flash.present? %>
  <div id="navbar-spacer"></div>
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
<% end %>
