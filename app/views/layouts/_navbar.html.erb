<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="\">Internet Vortex</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="<%= check_if_active('Jokes') %>"><%= link_to 'Jokes', jokes_path %></li>
        <li class="<%= check_if_active('Articles') %>"><%= link_to 'Articles', articles_path %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">More Stuff<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="/about">About</a></li>
            <li><a href="/contact">Contact</a></li>
            <li class="divider"></li>
            <li class="dropdown-header">Archive</li>
            <li><%= link_to "Jokes Archive", "/archive/jokes/#{Date.today.strftime('%B-%d-%Y')}" %></li>
            <li><%= link_to "Article Archive", "/archive/jokes/#{Date.today.strftime('%B-%d-%Y')}" %></li>
          </ul>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>

<% if flash.present? %>
  <div id="navbar-spacer"></div>
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
<% end %>
